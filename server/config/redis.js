// config/redis.js (pour plus tard - caching et sessions)
class RedisClient {
    constructor() {
        this.client = null;
    }
    // À implémenter semaine 3-4
}

// config/cloudinary.js (pour les uploads d'images)
class CloudinaryConfig {
    constructor() {
        // Configurer cloudinary
    }
}